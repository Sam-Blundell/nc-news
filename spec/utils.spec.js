const { expect } = require('chai');
const {
  formatDates,
  makeRefObj,
  formatComments,
} = require('../db/utils/utils');

// describe('formatDates', () => {
//   it('should return an empty array when passed an empty array', () => {
//     expect(formatDates([])).to.deep.equal([]);
//   });
//   it('should return an array containing a javascript date object when passed an array containing a UNIX timestamp', () => {
//     const timeStamps = [{ time: '2019-12-09T14:16:24.541Z' }];
//     const input = formatDates(timeStamps);
//     const actual = [{ time: '2019-12-09 14:16:24' }];
//     expect(input).to.deep.equal(actual);
//   });
//   it('should return an array containing javascript date objects when passed an array of objects containing UNIX timestamps', () => {
//     const timeStamps = [
//       {},
//       {},
//       {}
//     ]
//     const input = formatDates(timeStamps);
//     const actual = [
//       {},
//       {},
//       {}
//     ]
//   })
// });

describe('makeRefObj', () => { });

describe('formatComments', () => { });
